# Issue tracker: GitHub

本仓库的任务和需求使用 GitHub Issues 管理，所有操作使用 `gh` CLI。

## 约定

- 创建、查询、评论、标记和关闭 Issue 时，使用对应的 `gh issue` 命令。
- 技能要求“发布到任务系统”时，创建 GitHub Issue。
- 技能要求“读取相关任务”时，同时读取 Issue 正文、评论和标签。

## Pull requests as a triage surface

外部 PR 不作为需求分类入口。
